Django==6.0.6
django-environ==0.13.0
django-debug-toolbar==6.3.0
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
drf-spectacular==0.29.0
msal==1.36.0
httpx==0.28.1
django-filter==25.2
drf-nested-routers==0.95.0
django-modeltranslation==0.20.3
nh3==0.3.5                   # Rust/ammonia HTML sanitizer (replaced bleach) — tools/html_sanitizer.py
icalendar==7.1.2             # RFC 5545 .ics generation for the per-user calendar feed
anthropic==0.97.0
firebase-admin==7.4.0
boto3==1.35.99               # AWS SDK — S3 presigned URLs for file attachments (instance role creds)
psycopg[binary]>=3.2,<4      # PostgreSQL driver (prod; sqlite/dev needs nothing)

# --- prod serving / observability (OPERATIONS.md §3.3 / §3.8) ---
gunicorn==26.0.0
sentry-sdk==2.20.0           # opt-in error tracking (enabled when SENTRY_DSN set + STATE=PROD)
