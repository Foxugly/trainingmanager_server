# Training Manager — Deep Audit (2026-06)

Full read-only audit of both repos by a multi-agent pass:
- Backend: `trainingmanager_server` (Django 6.0 / DRF, PostgreSQL, ~855 tests)
- Frontend: `trainingmanager_frontend` (Angular 21 / PrimeNG / Transloco)

Overall the codebase is **high quality**: 100% OnPush, `@for track` everywhere,
disciplined owner/manager/athlete authorization, dashboard/stats already
de-N+1'd, fail-loud settings, throttling/Turnstile/RGPD. Items below are the
genuine gaps, prioritized. Status is tracked as items are addressed.

Legend: ☐ open · ☑ done · ⏸ deferred (with reason)

## Autonomous session progress (2026-06-09/10)

Worked through the audit unattended, landing only CI-gated, low-risk changes and
**deferring anything risky on an unsupervised prod** (schema migrations — the
deploy has no auto-rollback; the JWT-cookie auth refactor; CSP; infra-bound items).

**Shipped & deployed (all green):**
- **All 5 backend P0** security/integrity fixes, each with a regression test.
- Frontend **a11y + public toasts** (P0 toast, skip-link, admin landmark + button labels).
- CI **OpenAPI schema-drift guard** + deploy **health-gate**.
- **event-print N+1** removed (build from `rounds_detail`).

**Deferred for a supervised pass** (see ⏸ below): all schema-migration items
(indexes, dedup constraint, recap idempotency), JWT→HttpOnly-cookie, CSP,
`bleach`→`nh3`, AI-spend alerting, and the magic-link single-use change (which is
intentionally replayable for email-scanner tolerance).

**Still open & safe to do next** (no migration / no auth-flow risk): multi-tab
form validation + unsaved-changes guard, `effect()`→`toObservable` in the 5 admin
lists, the admin-CRUD dedup refactor, and the new features below.

## P0 — security / data-integrity

- ☑ **Member create accepts arbitrary `user_id` (IDOR / account-linking).** Fixed:
  `perform_create` now requires the linked user to already belong to a team the
  caller manages (`member/views.py` `_require_user_in_managed_team`). Tests in
  `tests/test_member_create.py`.
- ☑ **RSVP → attendance bypasses the team-enabled-status invariant.** Fixed:
  `apply_to_attendance` resolves statuses from `team.attendance_statuses`; a
  non-enabled status is skipped. Regression test in `tests/test_rsvp.py`.
- ☑ **AI generate-training stamps Rounds with the wrong sport on multi-sport
  teams.** Fixed: stamps `event.sport or team.default_sport` (matches the
  generator). Regression test in `tests/test_ai_training.py`.
- ☑ **Unbounded bulk-attendance write.** Fixed: `MAX_BULK_ATTENDANCES = 500` cap
  in `AttendanceBulkSerializer`. Test in `tests/test_attendance.py`.
- ☑ **`RoundViewSet.clone`/`clone_exercise` no write-permission check / no
  `author`.** Fixed: both call `_require_may_mutate`; clone stamps `author`.
  IDOR regression test in `tests/test_lock_clone.py`.
- ☑ **Public-page toasts never render.** Fixed: `<p-toast>` added to PublicLayout
  (frontend repo). Shipped with the a11y batch.
- ☐ **Decide & document catalog sharing scope.** `Exercise`/`Round` reads are
  scoped by sport+language, not team → cross-team session contents readable.
  Mitigated for round *clone* (now team-gated, above); the *list/read* sharing
  model still needs a product decision. `round/views.py:32`, `exercise/views.py:198`.

## P1 — high value

Security
- ⏸ **JWT in `localStorage` → HttpOnly cookie.** Highest-value security change but
  a large, high-blast-radius auth refactor — **deferred for a supervised change**
  (not landed unattended). `token.storage.ts:9`.
- ⏸ **No CSP header.** Needs `django-csp` (a new dep + middleware + careful
  directives for Quill/Turnstile/Sentry) or an nginx change (managed out-of-band).
  **Deferred for a supervised change.** `settings/prod.py`.
- ⏸ **Magic-link / password-reset replayable until expiry.** **Deferred by design
  decision:** the replayable-15-min window is intentional so email link
  pre-fetchers (scanners) don't consume a strictly single-use token before the
  user clicks. Single-use would break that. Documented, not changed.

Frontend UX / a11y
- ☐ Multi-tab form validation silently swallowed; switch to first invalid tab +
  `markAllAsTouched()`. `teams-form.component.ts:587`, `events-form.component.ts:378`.
- ☐ No unsaved-changes guard (`CanDeactivate`) on the big editors.
- ☑ Admin icon-only delete/restore buttons `ariaLabel`; skip-link in both shells;
  admin `<main>` → labelled `<section>`. (a11y batch, shipped.)

Perf / contract
- ☑ `event-print` N+1 → builds from embedded `rounds_detail` (one request).
- ⏸ Sentry tracing in the initial bundle — the real bundle win depends on
  `@sentry/angular` tree-shaking (namespace import) and deferring all of init
  loses eager error capture. **Deferred pending a bundle measurement.**
- ☐ 32 endpoints `Observable<any>`; casts hide schema desyncs — add
  `@extend_schema(responses=)` + regen. (Now guarded against further drift, below.)

CI / ops / observability
- ☑ Backend deploy post-restart **health-gate** (`deploy/deploy.sh`): probes
  `/health/` after restart, exits non-zero (RED) if unhealthy. No auto-rollback
  (forward migrations make it unsafe — manual).
- ☑ **OpenAPI schema-drift guard** in CI (`deploy.yml` test job). Coverage floor
  still ☐ (needs a baseline measurement first).
- ⏸ AI spend budget alert — needs CloudWatch/infra. **Deferred (infra).**

## P2 — tech debt / polish

- ⏸ Missing indexes (`Program (team,-updated_at)`; drop redundant `audit.created_at`
  single index; AI `Exercise` dedup composite). **All require migrations →
  deferred for a supervised deploy** (the deploy has no auto-rollback, and the
  exercise-dedup *unique* constraint could fail on pre-existing duplicate rows).
- ☐ Duplicated admin CRUD ×4 (sports / energy-systems / segments / modalities) —
  pure refactor, safe; not yet done.
- ☐ `effect()`-driven data fetch in 5 list components → `toObservable→switchMap`.
- ⏸ `bleach` → `nh3` — dependency swap + sanitizer re-test; **supervised.**
- ☐ `weekly_recaps` N+1 + not idempotent (idempotency needs a sent-marker — small
  migration → supervised; the batch-query part is safe).
- ☐ Half-built dark mode; large smart components; `team.spec` flaky.

## New features (grounded in existing models)

- ⏸ **E1 Push / realtime** — EXCLUDED by request.
- ☐ **E2 AI "review / critique a training block"** (ROTI + attendance + performance).
- ☑ **E3 One-click RSVP from reminder email** — signed member-scoped token (72h),
  server-rendered two-step (GET confirm page → POST upsert), CSRF-exempt, link-prefetch
  safe. `rsvp/magic_rsvp.py` (token) + `rsvp/magic_views.py` (HTML view) + `rsvp-magic/<token>/`
  route; reminder email carries per-member Going/Maybe/Not-going links via a new
  `notify_many(email_extra_for=…)` hook (email-only, never on the in-app row).
- ☑ **E4 Extend attachments to programs / performance targets** (storage already generic).
- ☑ **E5 Audit retention policy** (batched purge command + systemd timer + `AUDIT_RETENTION_DAYS`).
- ☑ **E6 Athlete/team progress view** (ROTI chart + attendance streak on team-stats).
- ☑ **E7 Verified change-email flow** (allauth plumbing present).
